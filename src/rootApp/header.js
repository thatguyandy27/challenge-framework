import React from 'react';

const Header = props => {

    return (<header className="c-header">
                <h2 className="c-header-label">Interactive Javascript Validator</h2>
        </header>);
};

export default Header;